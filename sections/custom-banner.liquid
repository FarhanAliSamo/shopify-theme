{% schema %}
{
    "name": "Custom Banner",
    "settings": [
        {
            "type": "text",
            "id": "heading",
            "label": "Heading",
            "default": "That our customer banner "
        },
        {
            "type": "text",
            "id": "description",
            "label": "Description",
            "default": "that our custom dscription "
        }
    ]
}
{% endschema %}


<div>
    <h1>{{ section.settings.heading }}</h1>
    <p>{{ section.settings.description }}</p>
</div>